app = "be10x-nutrition-calorie-app"
primary_region = "ams" # Change to your region if different

[build]

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

